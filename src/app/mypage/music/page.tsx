import { MusicArchiveClient } from "@/features/music-archive/music-archive-client";

export const metadata = { title: "마이페이지 - 내 음악 관리" };

export default function MusicArchivePage() {
  return <MusicArchiveClient />;
}
